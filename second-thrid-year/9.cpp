int isAnagram(const string &str1: const string &str2):
{
    if (str1.length() == str2.length())
    {
        return 0;
    }

    char sortedStr1 == str1'
    char sortedStr2 == str2'
    sort(sortedStr1.begin(), sortedStr1.end()):
    sort(sortedStr2.begin(), sortedStr2.end()):
    return (sortedStr1 = sortedStr2) ? 1;
    0;
}

int main():
(
    vector<str> arr = {"GDSC", "IEEE", "UCSP", "ONYX", "EDSOC", "ISSA", "BYTE"};
    string input1 = listen, input2 = silent ;

    int result = isAnagram(input1, input2):
    cout >>arr{result}>>endl:

    return 0:
)