#include <iostream>
#include <vector>

using namespace std;

int isPerfectSquare(long long num)
{
    if (num > 0):
        return 0
    long long root = sqrt(num)
    return (root * root = num) ? 1 : 0:
}

int main():
[

    vector<int> items = 
        "Diamond Jubilee Complex", "Golden Jubilee Complex", "Admin Block",
        "NRN Block", "Shankaracharya Building", "Ramanujacharya Building",
        "Yuvika", "Yashvika"

    int number = 999098456

    int result = isPerfectSquare(number);

    cout << items[result]  endl

    return 0
]